<?php
$thermostatPlanifId = $args[0];
/** @var \Materialize\Button\FlatButton $submit */
$submit = $args[1];
$addButton = new \Materialize\FloatingActionButton([
    'id' => 'planif-add',
    'icon' => 'add',
    'wrapper' => 'col s6',
    'href' => '#',
    'fixed' => true
]);
$spinner = \Materialize\WidgetFactory::makeSpinner("spinner");

$method = ($thermostatPlanifId) ? 'put' : 'post';
$action = ($thermostatPlanifId) ? 'api/thermostat/planif/update' : 'api/thermostat/planif/add';

?>
<form class="row" action="<?= $action ?>" method="<?= $method ?>">
    <div id="scenario-content">
        <div class="center-align">
            <?= $spinner->getHtml(); ?>
        </div>
    </div>
    <input type="text" name="id" id="id" value="<?= $thermostatPlanifId ?>" hidden readonly required/>
    <?= $submit->getHtml(); ?>
    <?= $addButton->getHtml() ?>
</form>